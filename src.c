#include <stdio.h>

int main(){
 
    int sveta;
    for (sveta = 0; sveta < 10; sveta++) 
    {
        int dima;
        for (dima = 0; dima < 10; dima++)
        {
            printf("x");
        }
        printf("\n");
    }

    return 0;
}